# Core MCP server dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
httpx>=0.25.0
python-dotenv>=1.0.0
typing-extensions>=4.8.0

# HTTP and web scraping
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0  # Parser for BeautifulSoup (faster)

# Dependencies for visa tools
playwright>=1.48.0

# Dependencies for utilities tools
pytz>=2023.3  # Timezone support for date/time tool

# Dependencies for memory tools
qdrant-client>=1.9.0  # Qdrant vector database for long-term memory
sentence-transformers>=2.7.0  # Sentence transformers for embeddings
numpy>=1.26.0  # NumPy for vector operations
openai>=1.0.0  # OpenAI for memory extraction and LLM calls

# Database dependencies for planner tools
psycopg2-binary>=2.9.9  # PostgreSQL adapter
sqlalchemy>=2.0.0  # Database ORM

# Note: Flight tools use SerpAPI via direct HTTP requests (requests library), no separate package needed
# Note: Hotel tools use LiteAPI via direct HTTP requests (httpx library)

# Azure Blob Storage for API logging
azure-storage-blob>=12.19.0

